server:
  port: 8080

spring:
  application:
    name: backend

#  datasource:
#    url: jdbc:postgresql://localhost:5432/backend_db_flyway_v2
#    username: mac
#    password:

  datasource:
    url: jdbc:postgresql://localhost:5432/backend_db
    username: backend_user
    password: Backend123!

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  cors:
    allowed-origin: ${CORS_ALLOWED_ORIGIN:http://localhost:5173}
  upload:
    dir: ${UPLOAD_DIR:uploads}


jwt:
  secret: ${JWT_SECRET:bXktc3VwZXItc2VjcmV0LWtleS1mb3Itand0LXRva2VuLW15LXN1cGVyLXNlY3JldC1rZXk=}
  expiration: ${JWT_EXPIRATION:86400000}